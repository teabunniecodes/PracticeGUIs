#:kivy 2.1.0
#:set padding 20

FloatLayout:
    Board:
        id: board
        pos_hint: {'center_x': .5, 'center_y': .5}
        size_hint: None, None
        center: root.center
        size: [(min(root.width, root.height) - 2 * padding), (min(root.width, root.height) - 2 * padding)]

# <WordleLayout>: